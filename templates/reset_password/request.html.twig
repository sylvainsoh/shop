{% extends 'base.html.twig' %}

{% block title %}Réinitialiser votre mot de passe{% endblock %}

{% block body %}
"""""
    {{ include( "partials/title_section.html.twig", {'name' :'Réinitialisation'}) }}

   <div class="row">
       <div class="col-4 offset-4">
           <hr>
           {% for flash_error in app.flashes('reset_password_error') %}
               <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
           {% endfor %}
           <h4>Réinitialiser votre mot de passe</h4>

           {{ form_start(requestForm) }}
           {{ form_row(requestForm.email) }}
           <div class="text-center">
               <small>
                   Saisissez votre adresse électronique et nous vous enverrons un
                   lien pour réinitialiser votre mot de passe.
               </small>
           </div>

          <div class="text-center">
              <button class="btn btn-primary">Envoyer le courriel de réinitialisation du mot de passe</button>
              {{ form_end(requestForm) }}
          </div>
       </div>
   </div>
    <br>
{% endblock %}
